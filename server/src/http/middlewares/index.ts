import { globalErrorHandle } from './errorHandle';
import { jsonParse } from './json';
import { validationJwt } from './auth';

export default {
    globalErrorHandle,
    jsonParse,
    validationJwt,
};
