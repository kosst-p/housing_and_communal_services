import { Schema, Document, FilterQuery, SortOrder } from 'mongoose';

export const ObjectId = Schema.Types.ObjectId;
export { Document };
export { FilterQuery };
export { SortOrder };
export { IDBConfig } from '../config/index';
